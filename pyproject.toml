[project]
name = "miggo-tag-propagation"
version = "0.1.0"
description = "Rule-driven tag propagation across an entity graph in SQLite."
requires-python = ">=3.11"
dependencies = ["pydantic"]

[dependency-groups]
dev = ["pytest"]
